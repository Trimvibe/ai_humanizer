import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color backgroundDark = Color(0xFF050505);
  static const Color backgroundSlate = Color(0xFF0A0F1A);

  // Accents (Neon glow colors)
  static const Color neonCyan = Color(0xFF00F2FE);
  static const Color neonPurple = Color(0xFF8B5CF6);
  static const Color neonBlue = Color(0xFF3B82F6);

  // Text
  static const Color textPrimary = Color(0xFFF8FAFC);
  static const Color textSecondary = Color(0xFF94A3B8);

  // Surfaces
  static const Color glassSurface = Color(0x08FFFFFF); // ~3% opacity white
  static const Color glassBorder = Color(0x1AFFFFFF); // ~10% opacity white
}
